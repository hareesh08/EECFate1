<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- This allows access to the external storage directory -->
    <external-path
        name="external_files"
        path="." />

    <!-- This allows access to the Downloads directory where PDF files are stored -->
    <external-path
        name="downloads"
        path="Download/EECFate/" />
</paths>

